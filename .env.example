# Optional: public URL of your CV PDF. When set, all “CV” / “Download CV” links use this
# instead of `public/cv/mark-takla-cv.pdf`. Replace the file at the same URL (or use a
# stable “latest” link) so you don’t need to edit code—only redeploy if you change the URL.
# NEXT_PUBLIC_CV_URL=https://example.com/path/to/Mark-Takla-CV.pdf
